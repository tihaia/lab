//Распишите код для структуры данных динамического массива

struct DynamicArray{    //инициализируем структуру DynamicArray
    int* elements;        //инициализируем целостный указатель на массив элементов
    int length=0;           //инициализируем начальную/текущую длину массива, то есть количество элементов
    int capacity=0;           //указываем размер выделенной памяти под массив (capacity=емкость)
    DynamicArray(int);          //структура DynamicArray принимает емкость и длину вышеуказанного массива
    void coutElements();          //вывод элементов
};
